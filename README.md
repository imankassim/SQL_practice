# SQL_practice

The answers_sql.txt file contains the SQL queries used to find the answers to the challenges.

The answers.txt file contains the actual answers (or row count/row information if the actual answers were too long.)